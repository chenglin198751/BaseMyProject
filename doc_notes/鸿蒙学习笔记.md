# 鸿蒙学习笔记

**1、在@Component中获取Context：**
- private context = getContext(this) as common.UIAbilityContext;
- 获取缓存路径：this.context.cacheDir
	
**2、for of 语句，可遍历数组或字符串：**

	for (let ch of 'hello') {
	  
	}

**3、字符串拼接：**

	let s1 = 'Hello';
    let s2 = 'world';
	let z: string = `${s1} ${s2}`;
	
**4、可选参数：**
- 1. 可选参数的格式可为name?: Type

	function hello(name?: string) {
	  if (!name) {
		console.log('Hello!');
	  } else {
		console.log(`Hello, ${name}!`);
	  }
	}
	
- 2. 可选参数的另一种形式为设置的参数默认值。如果在函数调用中这个参数被省略了，则会使用此参数的默认值作为实参。

	function multiply(n: number, coeff: number = 2): number {
	  return n * coeff;
	}
	multiply(2);  // 返回2*2
	multiply(2, 3); // 返回2*3	

-3. 可选参数，指定参数名的写法：
	// Test.ets文件：
	declare interface Params {
	  url: string | Resource
	  title?: string
	  hideBar?: boolean
	  close?: () => void
	}

	export class Test {
	  static  show(params: Params) {
	  }
	}	
	
	调用：Test.show({ url: 'xxxx', hideBar: true })
	
-4. 往自定义的@Component里面传入回调方法onTabSelected(index: number)：

	1.定义：
	@Component
	export struct SlidingTabsV1 {
	onTabSelected?: (index: number) => void
	  2.然后在具体代码里调用this.onTabSelected?.(this.index)激发回调
	}
	3.外部调用组件时传入：
	SlidingTabsV1({
		 index:this.index2,onTabSelected:(index: number)=>{   
	   }
	})
	
**5、箭头函数（Lambda函数）可嵌套在别的正常函数内部：**	

	let sum = (x: number, y: number): number => {
	  return x + y;
	}
	
**6、接口可以继承：**	

	interface Style {
	  color: string;
	}

	interface ExtendedStyle extends Style {
	  width: number;
	}

**7、function和method不一样：**
- 在ArkTS中，function关键字定义的函数可以被定义在类的外部，但不能直接定义在类的内部。

	function myFunction() {
		console.log("");
	}

	class MyClass {
		// 类内部的方法
		myMethod() {
			console.log("");
		}
	}

	const instance = new MyClass();
	instance.myMethod();
	myFunction();


**8、空安全和判空检查：**		
1. 默认情况下，ArkTS中的所有类型都是不可为空的。但是可以为空值的变量定义为联合类型T | null。
	let x: number | null = null;
	
2. 关于在访问对象属性时判空的3种运算符：
	class Person  {
	  nick: string | null = null;
	}

	function foo(person: Person  | null) {
	  // 1.（官方名字：非空断言运算符）如果运行时a为空，则发生运行时异常
	  person!.nick;
	  
	  // 2.（官方名字：可选链运算符）如果运行时a为空，或者a.value为空，则可选链运算符会返回undefined
	  person?.nick
	  
	  // 3.（官方名字：空值合并运算符）a ?? b等价于三元运算符(a != null && a != undefined) ? a : b
	  person.nick ?? ''
	}

**9、数值计算使用TypedArray，TypedArray类型化数组是一种类似数组的对象：**
	
	const typedArray1 = new Int8Array([1, 2, 3]);  // 针对这一场景，建议不要使用new Array([1, 2, 3])
	const typedArray2 = new Int8Array([4, 5, 6]);  // 针对这一场景，建议不要使用new Array([4, 5, 6])
	const res = new Int8Array(3);
	for (let i = 0; i < 3; i++) {
	  res[i] = typedArray1[i] + typedArray2[i];
	}
	
**10、应用状态管理-数据共享：**	
- AppStorage是应用全局的UI状态存储
- LocalStorage是页面级的，通常应用于页面内的数据共享
- PersistentStorage是持久化的数据
- 以上这些数据是通过业务逻辑中实现，与UI解耦，如果希望这些数据在UI中使用，需要用到@StorageProp和@StorageLink。

**11、装饰器：**
- @Watch应用于对状态变量的监听。如果开发者需要关注某个状态变量的值是否改变，可以使用@Watch为状态变量设置回调函数。
- $$运算符为系统内置组件提供TS变量的引用，使得TS变量和系统内置组件的内部状态保持同步
- @Prop装饰器：父向子单向同步
- @Link装饰器：父子双向同步
- @Provide装饰器和@Consume装饰器：与后代组件双向同步

**12、component声明周期：**

	// 页面每次显示时触发一次，包括路由过程、应用进入前台等场景。
	// 只有被@Entry装饰的组件才可以调用页面的生命周期
	onPageShow() {
	}
	
	// 页面每次隐藏时触发一次，包括路由过程、应用进入后台等场景。
	// 只有被@Entry装饰的组件才可以调用页面的生命周期
	onPageHide() {
	}

	// 当用户点击返回按钮时触发。
	// 只有被@Entry装饰的组件才可以调用页面的生命周期
	onBackPress() {
	  // 返回true表示页面自己处理返回逻辑，不进行页面路由；
	  // 返回false表示使用默认的路由返回逻辑，不设置返回值按照false处理
	  return true 
	}

	// 组件即将出现时回调该接口，具体时机为在创建自定义组件的新实例后，在执行其build()函数之前执行。
	aboutToAppear() {
	}

	// 组件build()函数执行完成之后回调该接口，不建议在onDidBuild函数中更改状态变量、
	// 使用animateTo等功能，这可能会导致不稳定的UI表现。
	onDidBuild() {
	}

	// aboutToDisappear函数在自定义组件析构销毁之前执行。不允许在aboutToDisappear函数中改变状态变量，
	// 特别是@Link变量的修改可能会导致应用程序行为不稳定。
	aboutToDisappear() {
	}
	
**13、BuildProfile类是编译时产生的，里面包含了build-profile.json5的配置信息**	
	
**14、View相关**		
- 分割线（区分纵向横向）：Divider().vertical(true).height(22).color('#182431').opacity(0.6).margin({ left: 8, right: 8 })
- 空白填充组件：在容器主轴方向上，空白填充组件具有自动填充容器空余部分的能力。仅当父组件为Row/Column/Flex时生效。

**15、定时器（延时执行）**

	1.延迟执行：
	setTimeout(() => {
	  console.info('delay 1s');
	}, 1000);

	setTimeout(() => {
	  console.info('delay 1s');
	}, 1000);
	
	2.重复调用一个函数，在每次调用之间具有固定的时间延迟。
	setInterval(() => {
	  console.log('do every 1s.');
	}, 1000);	

	let intervalID = setInterval(() => {
	  console.log('do every 1s.');
	}, 1000);
	clearInterval(intervalID);